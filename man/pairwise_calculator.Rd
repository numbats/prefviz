% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwise.R
\name{pairwise_calculator}
\alias{pairwise_calculator}
\title{Compute pairwise results from ranked preference data}
\usage{
pairwise_calculator(x, preferences_col = NULL, frequency_col = NULL)
}
\arguments{
\item{x}{A \code{preferences} object, or a data frame / tibble containing a
\code{preferences}-typed column.}

\item{preferences_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
When \code{x} is a data frame, the column containing the preferences.
Passed directly to \code{\link[prefio:adjacency]{prefio::adjacency()}}.}

\item{frequency_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
Optional column containing ballot frequencies.
Passed directly to \code{\link[prefio:adjacency]{prefio::adjacency()}}.}
}
\value{
An S3 object of class \code{"pairwise"} with four components:
\describe{
\item{\code{pairwise_matrix}}{N×N integer matrix from \code{\link[prefio:adjacency]{prefio::adjacency()}}.}
\item{\code{two_candidate_preferred}}{Tibble with one row per pair,
columns: \code{item_a}, \code{item_b}, \code{wins_a}, \code{wins_b}, \code{total}, \code{tcp_a},
\code{tcp_b}, \code{h2h_winner}.}
\item{\code{condorcet_winner}}{Name of the Condorcet winner, or \code{NA} if none.}
\item{\code{condorcet_loser}}{Name of the Condorcet loser, or \code{NA} if none.}
}
}
\description{
Computes all pairwise comparisons from a set of ranked
preferences using \code{\link[prefio:adjacency]{prefio::adjacency()}}. For each pair of items, reports
the number of voters who preferred each item and the Two-Candidate Preferred
(TCP) ratio. Also identifies the Condorcet winner and loser where they exist.
}
\details{
\strong{TCP ratio} (\code{tcp_a}, \code{tcp_b}) is computed as \code{wins / (wins_a + wins_b)}.
The denominator is the number of voters who expressed a preference between
the specific pair, not the total number of voters. This correctly handles
partial rankings where some voters did not rank all items.

\strong{Condorcet winner}: the item that beats every other item head-to-head
(tcp > 0.5 in all pairs it appears in). At most one can exist.

\strong{Condorcet loser}: the item that loses to every other item head-to-head
(tcp < 0.5 in all pairs it appears in). At most one can exist. Neither
winner nor loser may exist when preference cycles are present.
}
\examples{
library(prefio)

prefs <- data.frame(
  A = c(1, 1, 1, 2, 2),
  B = c(2, 2, 3, 1, 3),
  C = c(3, 3, 2, 3, 1)
) |>
  wide_preferences(col = vote, ranking_cols = A:C)

result <- pairwise_calculator(prefs, preferences_col = vote)
print(result)

}
\seealso{
\code{\link[=pairwise_heatmap]{pairwise_heatmap()}} to visualise the results.
}
