% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quick_plots.R
\name{dop_bar}
\alias{dop_bar}
\title{Bar chart of preference distribution for one contest}
\usage{
dop_bar(data, items, value_col = NULL, round_col = "round", at_round = 1)
}
\arguments{
\item{data}{A data frame in wide or long format. See Details.}

\item{items}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
Columns to plot. Interpretation depends on format:
\itemize{
\item \strong{Wide}: A tidy-select expression identifying the item columns to pivot,
e.g. \code{ALP:Other} or \code{-c(round, winner)}.
\item \strong{Long}: A single column containing item names, e.g. \code{PartyAb}.
}}

\item{value_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
\strong{Long format only.} Column containing the numeric values to plot (vote
shares or counts). When \code{NULL} (default), wide format is assumed and this
argument is ignored.}

\item{round_col}{Character. Name of the column used to
identify rounds. Default \code{"round"}, matching \code{\link[=dop_irv]{dop_irv()}} output. Override
when your data uses a different column name (e.g., \code{"CountNumber"}).}

\item{at_round}{Integer. The round number to display. Default is \code{1}.}
}
\value{
A ggplot object.
}
\description{
Draws a bar chart showing how votes or preferences are distributed across
items (candidates, parties, options) in a single contest or round. Bars are
ordered from highest to lowest value.
}
\details{
\code{dop_bar()} accepts data in two formats, detected automatically via
\code{value_col}:

\strong{Wide format} (\code{value_col = NULL}, the default)

One row per round, one column per item. This is the direct output of
\code{\link[=dop_irv]{dop_irv()}}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{round | ALP  | LNP  | Other | winner
    1 | 0.40 | 0.35 | 0.25  | ALP
    2 | 0.52 | 0.48 | 0.00  | ALP
}\if{html}{\out{</div>}}

Supply the item columns via \code{items} (e.g., \code{ALP:Other} or
\code{-c(round, winner)}) and select the round to display with \code{at_round}.
Use \code{round_col} if your round column is named something other than
\code{"round"} (e.g., \code{round_col = "CountNumber"}).

\strong{Long format} (\code{value_col} provided)

One row per item, with the item name and its value in separate columns.
This is the format of \link{aecdop_2022} and similar raw electoral datasets:

\if{html}{\out{<div class="sourceCode">}}\preformatted{DivisionNm | CountNumber | PartyAb | CalculationValue
Adelaide   |           0 | ALP     |            0.40
Adelaide   |           0 | LNP     |            0.35
Adelaide   |           0 | Other   |            0.25
}\if{html}{\out{</div>}}

Supply the item name column via \code{items}, the value column via
\code{value_col}, and the round to display via \code{at_round}.
Use \code{round_col} if your round column is named something other than
\code{"round"} (e.g., \code{round_col = "CountNumber"}).
}
\examples{
library(ggplot2)

# Wide format: output of dop_irv()
votes <- prefio::preferences(c("A > B > C", "B > A > C", "C > B > A",
                                "A > B > C", "A > C > B"))
irv_result <- dop_irv(votes)
dop_bar(irv_result, items = -c(round, winner), at_round = 1)

# Long format: pre-filter to desired contest, then plot
long_df <- aecdop_2022 |>
  dplyr::filter(
    CalculationType == "Preference Percent",
    CountNumber == 0,
    DivisionNm == "Adelaide"
  )
dop_bar(long_df, items = PartyAb, value_col = CalculationValue)

}
\seealso{
\code{\link[=dop_irv]{dop_irv()}} to generate wide-format input from raw ballot data.
}
