% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwise.R
\name{pairwise_build_tcp}
\alias{pairwise_build_tcp}
\title{Build TCP tibble from adjacency matrix}
\usage{
pairwise_build_tcp(adj)
}
\arguments{
\item{adj}{N×N integer matrix from \code{\link[prefio:adjacency]{prefio::adjacency()}}.}
}
\value{
A tibble with columns \code{item_a}, \code{item_b}, \code{wins_a}, \code{wins_b},
\code{total}, \code{tcp_a}, \code{tcp_b}, \code{h2h_winner}.
}
\description{
Converts the raw N×N adjacency matrix from \code{\link[prefio:adjacency]{prefio::adjacency()}} into a
tidy tibble with one row per unordered pair \{A, B\}.
}
\details{
Matrix indexing note: \code{\link[prefio:preferences]{prefio::wide_preferences()}} treats ranking values as
descending (higher value = more preferred), which reverses the usual
adjacency convention. As a result \code{adj[a, b]} counts b's wins over a, so
\code{wins_a} is read from \code{adj[b, a]}.
}
\keyword{internal}
