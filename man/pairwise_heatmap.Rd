% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quick_plots.R
\name{pairwise_heatmap}
\alias{pairwise_heatmap}
\title{Heatmap of pairwise results}
\usage{
pairwise_heatmap(x, value = c("tcp", "count"))
}
\arguments{
\item{x}{A \code{pairwise} object returned by \code{\link[=pairwise_calculator]{pairwise_calculator()}}.}

\item{value}{\code{"tcp"} (default) or \code{"count"}. Controls the tile annotation:
\itemize{
\item \code{"tcp"}: shows the TCP percentage, e.g. \code{"56.8\%"}.
\item \code{"count"}: shows the raw vote count, e.g. \code{"2841"}.
}}
}
\value{
A ggplot object.
}
\description{
Plots a full N×N heatmap of pairwise results from a \code{\link[=pairwise_calculator]{pairwise_calculator()}}
object. Each cell shows how the row item performed against the column item.
Color always encodes the TCP ratio (green = win, red = lose, white = 50/50).
}
\examples{
library(prefio)

prefs <- data.frame(
  A = c(1, 1, 1, 2, 2),
  B = c(2, 2, 3, 1, 3),
  C = c(3, 3, 2, 3, 1)
) |>
  wide_preferences(col = vote, ranking_cols = A:C)

result <- pairwise_calculator(prefs, preferences_col = vote)
pairwise_heatmap(result, value = "tcp")
pairwise_heatmap(result, value = "count")

}
\seealso{
\code{\link[=pairwise_calculator]{pairwise_calculator()}} to compute the input object.
}
