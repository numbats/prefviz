% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{ordered_path_df}
\alias{ordered_path_df}
\title{Validate and order path data}
\usage{
ordered_path_df(
  data,
  group,
  order_by,
  decreasing = FALSE,
  na_method = c("drop_na", "drop_group")
)
}
\arguments{
\item{data}{A data frame containing at least an \code{order_by} column (and
optionally a \code{group} column created by ggplot2).}

\item{decreasing}{Logical. If \code{TRUE}, sort \code{order_by} in decreasing order;
otherwise in increasing order.}

\item{na_method}{Character string indicating how to handle missing values in
\code{order_by}. One of \code{"drop_na"} or \code{"drop_group"}.}
}
\value{
A data frame with the same columns as \code{data}, but potentially fewer
rows (after dropping rows or groups) and with rows reordered within each
group.
}
\description{
Internal helper to validate and reorder data
within each group according to the \code{order_by} aesthetic.
Used by \code{stat_ordered_path()}.
}
\keyword{internal}
